# Rinas Abdullah — Portfolio

A bilingual (English/Arabic) personal portfolio built with React, TypeScript, Vite, and Tailwind CSS.

## Run Locally

**Prerequisites:** Node.js 18+

1. Install dependencies:
   `npm install`
2. Run the dev server:
   `npm run dev`
3. Build for production:
   `npm run build`
